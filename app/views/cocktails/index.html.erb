<div class="container">
 <div class="row" style="padding-top: 100px;">
  <div class="col-12 col-md-6 col-lg-4">
    <% @cocktails.each do |cocktail| %>
    <%= render "shared/cocktail_card", locals: { cocktail: cocktail } %>
    <% end %>
  </div>
</div>
</div>
